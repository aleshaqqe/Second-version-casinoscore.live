const { SUPPORTED_LANGS, DEFAULT_LANG, UI_TRANSLATIONS } = require("./translations");
const { GAMES } = require("./games");

module.exports = {
  SUPPORTED_LANGS,
  DEFAULT_LANG,
  UI_TRANSLATIONS,
  GAMES
};